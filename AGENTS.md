# Agent Instructions

- Do not edit files under /docs or /build by default.
- Treat /docs and /build as generated/copied build outputs.
- Make source changes in the upstream authoring locations instead (for example /data, /framework, or root config/source files), then rely on the build process to propagate changes.
- Only edit /docs or /build when the user explicitly requests it.
